{
  "install": "yarn install",
  "start": "(sudo service docker start || service docker start || true) && bash packages/diex-utils/setup-dev-env.sh && npx nx database:reset diex-server",
  "terminals": [
    {
      "name": "Development Server",
      "command": "yarn start"
    }
  ]
}
