name: Spawn Diex App Dev Test
description: >
  Starts a Diex all-in-one test instance (server, worker, database, redis)
  using the diexcrm/diex-app-dev Docker image on port 2021.
  The server is available at http://localhost:2021 with seeded demo data.

inputs:
  diex-version:
    description: 'Diex Docker Hub image tag for diex-app-dev (e.g., "latest" or "v1.20.0").'
    required: false
    default: 'latest'

outputs:
  server-url:
    description: 'URL where the Diex test server can be reached'
    value: http://localhost:2021
  api-key:
    description: 'API key (type: API_KEY) for the seeded Diex dev workspace'
    value: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..bfQjfyN0NEtTCLE_xPyNcwonDzlSXFoP8kdCQTdnuDc

runs:
  using: 'composite'
  steps:
    - name: Start diex-app-dev-test container
      shell: bash
      run: |
        docker run -d \
          --name diex-app-dev-test \
          -p 2021:2021 \
          -e NODE_PORT=2021 \
          -e SERVER_URL=http://localhost:2021 \
          -e MARKETPLACE_CATALOG_SYNC_CRON_ENABLED=false \
          -e API_RATE_LIMITING_SHORT_LIMIT=100000 \
          -e API_RATE_LIMITING_LONG_LIMIT=100000 \
          diexcrm/diex-app-dev:${{ inputs.diex-version }}

        echo "Waiting for Diex test instance to become healthy…"
        TIMEOUT=180
        ELAPSED=0
        until curl -sf http://localhost:2021/healthz > /dev/null 2>&1; do
          if [ "$ELAPSED" -ge "$TIMEOUT" ]; then
            echo "::error::Diex did not become healthy within ${TIMEOUT}s"
            docker logs diex-app-dev-test 2>&1 | tail -80
            exit 1
          fi
          sleep 3
          ELAPSED=$((ELAPSED + 3))
          echo "  … waited ${ELAPSED}s"
        done
        echo "Diex test instance is ready at http://localhost:2021 (took ~${ELAPSED}s)"
