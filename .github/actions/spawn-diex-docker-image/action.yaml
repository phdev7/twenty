name: Spawn Diex Docker Image
description: >
  Starts a full Diex instance (server, worker, database, redis) using Docker
  Compose. The server is available at http://localhost:3000 for subsequent steps
  in the caller's job.
  Accepts "latest" (pulls the latest Docker Hub image, checks out main) or a
  semver tag (e.g., v0.40.0).
  Designed to be consumed from external repositories (e.g., diex-app).

inputs:
  diex-version:
    description: 'Diex Docker Hub image tag — either "latest" or a semver tag (e.g., v0.40.0).'
    required: true
  diex-repository:
    description: 'Diex repository to checkout docker compose files from.'
    required: false
    default: 'diexhq/diex'
  github-token:
    description: 'GitHub token for cross-repo checkout. Required when calling from an external repository.'
    required: false
    default: ${{ github.token }}

outputs:
  server-url:
    description: 'URL where the Diex server can be reached'
    value: http://localhost:3000
  access-token:
    description: 'Admin access token for the Diex instance'
    value: ${{ steps.admin-token.outputs.access-token }}

runs:
  using: 'composite'
  steps:
    - name: Resolve version
      id: resolve
      shell: bash
      run: |
        VERSION="${{ inputs.diex-version }}"
        if [ "$VERSION" = "latest" ]; then
          echo "docker-tag=latest" >> "$GITHUB_OUTPUT"
          echo "git-ref=main" >> "$GITHUB_OUTPUT"
        elif echo "$VERSION" | grep -qE '^v[0-9]+\.[0-9]+\.[0-9]+$'; then
          echo "docker-tag=$VERSION" >> "$GITHUB_OUTPUT"
          echo "git-ref=$VERSION" >> "$GITHUB_OUTPUT"
        else
          echo "::error::diex-version must be \"latest\" or a semver tag (e.g., v0.40.0). Got: '$VERSION'"
          exit 1
        fi

    - name: Checkout docker compose files
      uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
      with:
        repository: ${{ inputs.diex-repository }}
        ref: ${{ steps.resolve.outputs.git-ref }}
        token: ${{ inputs.github-token }}
        sparse-checkout: |
          packages/diex-docker
        sparse-checkout-cone-mode: false
        path: .diex-spawn

    - name: Prepare environment
      shell: bash
      working-directory: ./.diex-spawn/packages/diex-docker
      run: |
        cp .env.example .env
        echo "" >> .env
        echo "TAG=${{ steps.resolve.outputs.docker-tag }}" >> .env
        echo "APP_SECRET=replace_me_with_a_random_string" >> .env
        echo "SERVER_URL=http://localhost:3000" >> .env

    - name: Start Diex instance
      shell: bash
      working-directory: ./.diex-spawn/packages/diex-docker
      run: |
        docker compose up -d --wait || {
          echo "::error::Docker compose failed to start or health checks timed out"
          docker compose logs
          exit 1
        }
        echo "Diex instance is ready at http://localhost:3000"

    - name: Set admin access token
      id: admin-token
      shell: bash
      run: |
        ACCESS_TOKEN="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..HMGqCsVlOAPVUBhKSGlD1X86VoHKt4LIUtET3CGIdik"
        echo "::add-mask::$ACCESS_TOKEN"
        echo "access-token=$ACCESS_TOKEN" >> "$GITHUB_OUTPUT"
