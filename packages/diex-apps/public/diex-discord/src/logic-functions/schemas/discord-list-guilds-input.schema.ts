import { type InputJsonSchema } from 'diex-sdk/logic-function';

export const discordListGuildsInputSchema: InputJsonSchema = {
  type: 'object',
  properties: {},
  required: [],
  additionalProperties: false,
};
