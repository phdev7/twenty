apiVersion: v1
kind: PersistentVolume
metadata:
  name: diexcrm-server-pv
  namespace: diexcrm
spec:
  storageClassName: default
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
