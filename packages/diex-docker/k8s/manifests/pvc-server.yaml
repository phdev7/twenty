apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: diexcrm-server-pvc
  namespace: diexcrm
spec:
  storageClassName: default
  volumeName: diexcrm-server-pv
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
