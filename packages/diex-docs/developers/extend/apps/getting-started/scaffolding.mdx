---
title: Scaffolding
description: Generate entity files interactively with yarn diex dev:add — objects, fields, views, logic functions, and more.
icon: "wand-magic-sparkles"
---

Instead of creating entity files by hand, use the interactive scaffolder:

```bash filename="Terminal"
yarn diex dev:add
```

It prompts you to pick an entity type and walks you through the required fields, then writes a ready-to-use file with a stable `universalIdentifier` and the correct `defineEntity()` call.

You can also pass the entity type directly to skip the first prompt:

```bash filename="Terminal"
yarn diex dev:add object
yarn diex dev:add logicFunction
yarn diex dev:add frontComponent
```

## Available entity types

| Entity type | Command | Generated file |
|-------------|---------|----------------|
| Object | `yarn diex dev:add object` | `src/objects/<name>.ts` |
| Field | `yarn diex dev:add field` | `src/fields/<name>.ts` |
| Logic function | `yarn diex dev:add logicFunction` | `src/logic-functions/<name>.ts` |
| Front component | `yarn diex dev:add frontComponent` | `src/front-components/<name>.tsx` |
| Role | `yarn diex dev:add role` | `src/roles/<name>.ts` |
| Skill | `yarn diex dev:add skill` | `src/skills/<name>.ts` |
| Agent | `yarn diex dev:add agent` | `src/agents/<name>.ts` |
| View | `yarn diex dev:add view` | `src/views/<name>.ts` |
| Navigation menu item | `yarn diex dev:add navigationMenuItem` | `src/navigation-menu-items/<name>.ts` |
| Page layout | `yarn diex dev:add pageLayout` | `src/page-layouts/<name>.ts` |
| Page layout tab | `yarn diex dev:add pageLayoutTab` | `src/page-layout-tabs/<name>.ts` |
| Command menu item | `yarn diex dev:add commandMenuItem` | `src/command-menu-items/<name>.ts` |
| View field | `yarn diex dev:add viewField` | `src/view-fields/<name>.ts` |
| Connection provider | `yarn diex dev:add connectionProvider` | `src/connection-providers/<name>.ts` |

## What the scaffolder generates

Each entity type has its own template. For example, `yarn diex dev:add object` asks for:

1. **Name (singular)** — e.g., `invoice`
2. **Name (plural)** — e.g., `invoices`
3. **Label (singular)** — auto-populated from the name (e.g., `Invoice`)
4. **Label (plural)** — auto-populated (e.g., `Invoices`)
5. **Create a view and navigation item?** — if you answer yes, the scaffolder also generates a matching view and sidebar link for the new object.

Other entity types have simpler prompts — most only ask for a name.

The `field` entity type is more detailed: it asks for the field name, label, type (from a list of all available field types like `TEXT`, `NUMBER`, `SELECT`, `RELATION`, etc.), and the target object's `universalIdentifier`.

## Custom output path

Use the `--path` flag to place the generated file in a custom location:

```bash filename="Terminal"
yarn diex dev:add logicFunction --path src/custom-folder
```
