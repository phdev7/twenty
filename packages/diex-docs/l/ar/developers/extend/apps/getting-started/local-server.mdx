---
title: الخادم المحلي
description: إدارة خادم Diex المحلي المستند إلى Docker — بدء التشغيل، الإيقاف، الترقية، مثيل اختبار متوازٍ، وإعداد SDK اليدوي.
icon: server
---

## إدارة الخادم المحلي

استخدم `yarn diex docker:*` للتحكّم في حاوية Diex المحلية:

| أمر                                    | ماذا يفعل                                          |
| -------------------------------------- | -------------------------------------------------- |
| `yarn diex docker:start`             | بدء تشغيل الخادم (يسحب الصورة إذا لزم الأمر)       |
| `yarn diex docker:start 2.2.0`       | بدء إصدار محدد من الخادم                           |
| `yarn diex docker:start --port 3030` | بدء التشغيل على منفذ مخصّص                         |
| `yarn diex docker:stop`              | إيقاف الخادم (مع الحفاظ على البيانات)              |
| `yarn diex docker:status`            | عرض عنوان URL والإصدار وبيانات اعتماد تسجيل الدخول |
| `yarn diex docker:logs`              | بث سجلات الخادم                                    |
| `yarn diex docker:reset`             | مسح البيانات والبدء من جديد                        |
| `yarn diex docker:upgrade`           | سحب أحدث صورة `diex-app-dev`                     |
| `yarn diex docker:upgrade 2.2.0`     | الترقية إلى إصدار محدد                             |

تظل البيانات محفوظة عبر عمليات إعادة التشغيل في وحدتي تخزين Docker (`diex-app-dev-data` لـ PostgreSQL، و`diex-app-dev-storage` للملفات). استخدم `reset` لمسح كل شيء.

## تثبيت إصدار الخادم

عند عدم تمرير أي إصدار، يقوم `docker:start` باشتقاق الإصدار من النطاق `engines.diex` في ملف `package.json` لتطبيقك — وهو نفس النطاق الذي يتحقق منه الخادم عند تثبيت تطبيقك. يشغّل أحدث صورة منشورة لـ `diex-app-dev` التي تُلبي هذا النطاق، مع الرجوع إلى `latest` عندما يكون الحقل غير موجودًا أو لا يتطابق أي إصدار منشور:

```json filename="package.json"
{
  "engines": {
    "diex": ">=2.2.0"
  }
}
```

مرِّر إصدارًا بشكلٍ صريح لتجاوز النطاق لتشغيلٍ واحد فقط: `yarn diex docker:start 2.3.0`. إذا كانت هناك حاوية موجودة بالفعل على إصدار مختلف، يقوم `docker:start` بترقيتها في مكانها (مع إعادة إنشاء الحاوية مع الحفاظ على وحدات تخزين بياناتك).

## ترقية صورة الخادم

يقوم `yarn diex docker:upgrade` بسحب أحدث صورة، ومقارنة التجزئات، ولا يعيد إنشاء الحاوية إلا إذا حدث تغيير فعلي. تظل وحدات التخزين محفوظة — ويتم استبدال الحاوية فقط. إذا تم سحب صورة جديدة وكانت الحاوية تعمل، فستبدأ عملية الترقية تلقائيًا حاوية جديدة؛ شغّل بعد ذلك `yarn diex docker:start` للانتظار حتى تصبح سليمة.

```bash filename="Terminal"
yarn diex docker:upgrade            # Latest
yarn diex docker:upgrade 2.2.0      # Specific version
```

تحقّق من الإصدار الجاري باستخدام `yarn diex docker:status` (يعرض قيمة `APP_VERSION` المضمنة في الحاوية).

## تشغيل مثيل اختبار متوازٍ

مرّر `--test` إلى أي أمر `docker:*` لإدارة مثيل ثانٍ معزول تمامًا — مفيد لاختبارات التكامل أو للتجربة من دون لمس بيانات التطوير الرئيسية لديك:

| أمر                                 | ماذا يفعل                                 |
| ----------------------------------- | ----------------------------------------- |
| `yarn diex docker:start --test`   | بدء مثيل الاختبار (المنفذ الافتراضي 2021) |
| `yarn diex docker:stop --test`    | إيقافه                                    |
| `yarn diex docker:status --test`  | عرض حالته                                 |
| `yarn diex docker:logs --test`    | بث سجلاته                                 |
| `yarn diex docker:reset --test`   | مسح بياناته                               |
| `yarn diex docker:upgrade --test` | ترقية صورته                               |

يملك مثيل الاختبار حاويته الخاصة (`diex-app-dev-test`) ووحدات التخزين الخاصة به (`diex-app-dev-test-data`، `diex-app-dev-test-storage`) وتهيئته — ويعمل جنبًا إلى جنب مع مثيلك الرئيسي بدون تعارضات. اجمع `--test` مع `--port` لتجاوز المنفذ 2021.

## إعداد يدوي (بدون أداة توليد الهيكل)

تجاوز أداة توليد الهيكل إذا كنت تضيف SDK إلى مشروع قائم:

```bash filename="Terminal"
yarn add diex-sdk diex-client-sdk
```

أضِف النص البرمجي إلى `package.json`:

```json filename="package.json"
{
  "scripts": {
    "diex": "diex"
  }
}
```

يمكنك الآن تشغيل `yarn diex dev`، و`yarn diex docker:start`، والبقية.

<Note>
لا تثبّت `diex-sdk` عالميًا — ثبِّته لكل مشروع بحيث يستخدم كل تطبيق إصداره الخاص.
</Note>
