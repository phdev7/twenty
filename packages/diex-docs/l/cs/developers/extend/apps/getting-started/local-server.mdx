---
title: Lokální server
description: Spravujte lokální Docker server Diex – spouštění, zastavení, upgrade, paralelní testovací instance a ruční nastavení SDK.
icon: server
---

## Správa lokálního serveru

K ovládání lokálního kontejneru Diex použijte `yarn diex docker:*`:

| Příkaz                                 | K čemu slouží                                  |
| -------------------------------------- | ---------------------------------------------- |
| `yarn diex docker:start`             | Spustí server (v případě potřeby stáhne image) |
| `yarn diex docker:start 2.2.0`       | Spustit konkrétní verzi serveru                |
| `yarn diex docker:start --port 3030` | Spustí na vlastním portu                       |
| `yarn diex docker:stop`              | Zastaví server (zachová data)                  |
| `yarn diex docker:status`            | Zobrazí URL, verzi a přihlašovací údaje        |
| `yarn diex docker:logs`              | Streamuje protokoly serveru                    |
| `yarn diex docker:reset`             | Vymaže data a začne znovu                      |
| `yarn diex docker:upgrade`           | Stáhne nejnovější image `diex-app-dev`       |
| `yarn diex docker:upgrade 2.2.0`     | Aktualizuje na konkrétní verzi                 |

Data přetrvávají při restartech ve dvou svazcích Dockeru (`diex-app-dev-data` pro PostgreSQL, `diex-app-dev-storage` pro soubory). Pomocí `reset` vymažte vše.

## Fixace verze serveru

Když není předána žádná verze, `docker:start` určí verzi z rozsahu `engines.diex` vaší aplikace v `package.json` — stejného rozsahu, vůči kterému server ověřuje, když je vaše aplikace nainstalována. Spustí nejnovější publikovaný image `diex-app-dev`, který splňuje tento rozsah, a pokud pole chybí nebo žádná publikovaná verze neodpovídá, použije `latest`:

```json filename="package.json"
{
  "engines": {
    "diex": ">=2.2.0"
  }
}
```

Předáním verze můžete rozsah pro jedno spuštění přepsat: `yarn diex docker:start 2.3.0`. Pokud už kontejner existuje v jiné verzi, `docker:start` jej na místě aktualizuje (znovu vytvoří kontejner při zachování vašich datových svazků).

## Aktualizace obrazu serveru

`yarn diex docker:upgrade` stáhne nejnovější image, porovná digesty a znovu vytvoří kontejner pouze v případě, že se skutečně něco změnilo. Svazky zůstanou zachovány — nahradí se pouze kontejner. Pokud byl stažen nový image a kontejner běžel, upgrade automaticky spustí nový kontejner; poté spusťte `yarn diex docker:start`, abyste počkali, než bude ve stavu 'healthy'.

```bash filename="Terminal"
yarn diex docker:upgrade            # Latest
yarn diex docker:upgrade 2.2.0      # Specific version
```

Běžící verzi ověříte pomocí `yarn diex docker:status` (zobrazí `APP_VERSION` zabudovanou v kontejneru).

## Spuštění paralelní testovací instance

Předejte `--test` libovolnému příkazu `docker:*` pro správu druhé, plně izolované instance — užitečné pro integrační testy nebo experimentování bez zásahu do vašich hlavních vývojových dat:

| Příkaz                              | K čemu slouží                                    |
| ----------------------------------- | ------------------------------------------------ |
| `yarn diex docker:start --test`   | Spustí testovací instanci (výchozí port je 2021) |
| `yarn diex docker:stop --test`    | Zastaví ji                                       |
| `yarn diex docker:status --test`  | Zobrazí její stav                                |
| `yarn diex docker:logs --test`    | Streamuje její protokoly                         |
| `yarn diex docker:reset --test`   | Vymaže její data                                 |
| `yarn diex docker:upgrade --test` | Aktualizuje její image                           |

Testovací instance má vlastní kontejner (`diex-app-dev-test`), svazky (`diex-app-dev-test-data`, `diex-app-dev-test-storage`) a konfiguraci — běží souběžně s vaší hlavní instancí bez konfliktů. Zkombinujte `--test` s `--port` pro změnu výchozího portu 2021.

## Ruční nastavení (bez generátoru kostry)

Pokud přidáváte SDK do existujícího projektu, generátor kostry přeskočte:

```bash filename="Terminal"
yarn add diex-sdk diex-client-sdk
```

Přidejte skript do `package.json`:

```json filename="package.json"
{
  "scripts": {
    "diex": "diex"
  }
}
```

Nyní můžete spouštět `yarn diex dev`, `yarn diex docker:start` a další.

<Note>
Neinstalujte `diex-sdk` globálně — nainstalujte jej v každém projektu zvlášť, aby každá aplikace používala svou vlastní verzi.
</Note>
