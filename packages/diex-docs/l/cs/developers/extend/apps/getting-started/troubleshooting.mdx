---
title: Řešení potíží
description: Časté problémy při prvním spuštění — Docker, verze Node, Yarn, závislosti.
icon: wrench
---

* **Chyby Dockeru** — Před spuštěním `yarn diex docker:start` se ujistěte, že Docker Desktop (nebo démon) běží. Chybová zpráva ukáže správný příkaz pro spuštění pro váš operační systém.
* **Špatná verze Node** — Potřebujete verzi 24.5+ (`engines.node: ^24.5.0`). Ověřte pomocí `node -v`.
* **Chybí Yarn 4** — Spusťte `corepack enable`.
* **Rozbité závislosti** — `rm -rf node_modules && yarn install`.
* **Chyby `diex-sdk` po upgradu na v2.8.0** — V 2.8.0 byl přesunut z `dependencies` do `devDependencies`. Viz [Struktura projektu → Závislosti](/l/cs/developers/extend/apps/getting-started/project-structure#dependencies).
* **`diex dev:build` upozorňuje na `diex-client-sdk` v sekci `dependencies`** — je poskytován za běhu Diex, takže by měl být přesunut do `devDependencies` vedle `diex-sdk`. Viz [Struktura projektu → Závislosti](/l/cs/developers/extend/apps/getting-started/project-structure#dependencies).

Zasekli jste se? Zeptejte se na [Discordu Diex](https://discord.com/channels/1130383047699738754/1130386664812982322).
