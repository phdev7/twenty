---
title: CLI
description: Comandi di `yarn diex` per eseguire funzioni, eseguire lo streaming dei log, gestire le installazioni delle app e passare da un remoto all'altro.
icon: terminal
---

La CLI `yarn diex` è la tua interfaccia per tutto ciò che riguarda le app. Elenco completo dei comandi:

| Comando                                         | Cosa fa                                                                 | Documentato in                                                                                                |
| ----------------------------------------------- | ----------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------- |
| `dev`                                           | Monitora i file sorgente e sincronizza in tempo reale le modifiche      | [Guida rapida](/l/it/developers/extend/apps/getting-started/quick-start)                                           |
| `piano`                                         | Visualizza in anteprima le modifiche ai metadati senza applicarle       | [Sincronizzazione e ripristino](/l/it/developers/extend/apps/operations/sync-and-recovery#previewing-changes-plan) |
| `applica`                                       | Applica le modifiche ai metadati dopo aver mostrato il piano            | [Sincronizzazione e ripristino](/l/it/developers/extend/apps/operations/sync-and-recovery)                         |
| `dev:build`                                     | Compila l'app e genera il client API (`--tarball` per creare un `.tgz`) | [Pubblicazione](/l/it/developers/extend/apps/operations/publishing)                                                |
| `dev:typecheck`                                 | Esegui il controllo dei tipi TypeScript                                 | [Test](/l/it/developers/extend/apps/operations/testing)                                                            |
| `dev:add`                                       | Genera lo scaffold per una nuova entità                                 | [Scaffolding](/l/it/developers/extend/apps/getting-started/scaffolding)                                            |
| `dev:generate-client`                           | Rigenera il client API tipizzato                                        | questa pagina                                                                                                 |
| `dev:function:exec` / `dev:function:logs`       | Esegui le funzioni e trasmetti in streaming i relativi log              | questa pagina                                                                                                 |
| `dev:translations-extract`                      | Estrai le stringhe traducibili nei cataloghi in `locales/`              | [Traduzioni](/l/it/developers/extend/apps/translations/overview)                                                   |
| `dev:catalog-sync`                              | Attiva la sincronizzazione del catalogo del marketplace                 | [Pubblicazione](/l/it/developers/extend/apps/operations/publishing#how-marketplace-discovery-works)                |
| `app:publish` / `app:install` / `app:uninstall` | Ciclo di vita delle release                                             | [Pubblicazione](/l/it/developers/extend/apps/operations/publishing) e questa pagina                                |
| `docker:*`                                      | Gestisci il container del server Diex locale                          | [Server locale](/l/it/developers/extend/apps/getting-started/local-server)                                         |
| `remote:*`                                      | Gestisci le connessioni al server                                       | questa pagina                                                                                                 |

Ogni comando accetta `-r, --remote \<name>` per puntare a un remoto specifico anziché a quello predefinito.

## Esecuzione delle funzioni (`yarn diex dev:function:exec`)

Esegui manualmente una funzione logica senza attivarla tramite HTTP, cron o evento del database:

```bash filename="Terminal"
# Execute by function name
yarn diex dev:function:exec -n create-new-post-card

# Execute by universalIdentifier
yarn diex dev:function:exec -u e56d363b-0bdc-4d8a-a393-6f0d1c75bdcf

# Pass a JSON payload
yarn diex dev:function:exec -n create-new-post-card -p '{"name": "Hello"}'

# Execute the install and uninstall hooks
yarn diex dev:function:exec --postInstall
yarn diex dev:function:exec --preInstall
yarn diex dev:function:exec --uninstall
```

## Visualizzazione dei log delle funzioni (`yarn diex dev:function:logs`)

Esegui lo streaming dei log di esecuzione per le funzioni logiche della tua app:

```bash filename="Terminal"
# Stream all function logs
yarn diex dev:function:logs

# Filter by function name
yarn diex dev:function:logs -n create-new-post-card

# Filter by universalIdentifier
yarn diex dev:function:logs -u e56d363b-0bdc-4d8a-a393-6f0d1c75bdcf
```

<Note>
Questo è diverso da `yarn diex docker:logs`, che mostra i log del container Docker. `yarn diex dev:function:logs` mostra i log di esecuzione delle funzioni della tua app dal server Diex.
</Note>

## Generare il client tipizzato (`yarn diex dev:generate-client`)

Rigenera il client API tipizzato (`diex-client-sdk`) dallo schema del remoto attivo, senza eseguire la build né sincronizzare un'app. Usalo per ottenere un client tipizzato in qualsiasi progetto — ad esempio un servizio backend che vive in un repository separato — che comunica con la tua istanza Diex:

```bash filename="Terminal"
# In your project (no Diex app definition required)
yarn add diex-sdk diex-client-sdk

# Connect to the Diex instance to generate the client from
yarn diex remote:add

# Generate the typed client into node_modules/diex-client-sdk
yarn diex dev:generate-client
```

Quindi importa il client nel tuo codice:

```typescript
import { CoreApiClient } from 'diex-client-sdk/core';
```

Esegui nuovamente il comando ogni volta che il tuo modello di dati cambia, per aggiornare i tipi generati.

<Note>
Il client viene generato all'interno di `node_modules`, quindi non viene inserito nel commit insieme al tuo codice. Esegui `yarn diex dev:generate-client` dopo ogni installazione (ad esempio in uno script `postinstall` o nella CI).
</Note>

## Disinstallazione di un'app (`yarn diex app:uninstall`)

Rimuovi la tua app dallo spazio di lavoro attivo:

```bash filename="Terminal"
yarn diex app:uninstall

# Skip the confirmation prompt
yarn diex app:uninstall --yes
```

## Gestione dei remoti

Un **remoto** è un server Diex a cui la tua app si connette. Durante la configurazione, lo strumento di scaffolding ne crea uno automaticamente per te. Puoi aggiungere altri remoti o passare da uno all'altro in qualsiasi momento.

```bash filename="Terminal"
# Add a new remote (opens a browser for OAuth login)
yarn diex remote:add

# Connect to a local Diex server (auto-detects port 2020 or 3000)
yarn diex remote:add --local

# Add a remote non-interactively (useful for CI)
yarn diex remote:add --url https://your-diex-server.com --api-key $DIEX_API_KEY --as my-remote

# List all configured remotes
yarn diex remote:list

# Set the active remote
yarn diex remote:use <name>

# Check that the active remote's authentication is still valid
yarn diex remote:status

# Remove a remote
yarn diex remote:remove <name>
```

Le tue credenziali sono archiviate in `~/.diex/config.json`.
