---
title: Resolução de Problemas
description: Problemas comuns na primeira execução — Docker, versão do Node, Yarn, dependências.
icon: wrench
---

* **Erros do Docker** — Certifique-se de que o Docker Desktop (ou o daemon) esteja em execução antes de `yarn diex docker:start`. A mensagem de erro mostrará o comando de inicialização correto para o seu sistema operacional.
* **Versão errada do Node** — É necessário 24.5+ (`engines.node: ^24.5.0`). Verifique com `node -v`.
* **Falta o Yarn 4** — Execute `corepack enable`.
* **Dependências com problemas** — `rm -rf node_modules && yarn install`.
* **Erros do `diex-sdk` após a atualização para a v2.8.0** — Ele foi movido de `dependencies` para `devDependencies` na v2.8.0. Veja [Estrutura do projeto → Dependências](/l/pt/developers/extend/apps/getting-started/project-structure#dependencies).
* **`diex dev:build` emite um aviso sobre `diex-client-sdk` em `dependencies`** — Ele é fornecido em tempo de execução pela Diex, então deve ser movido para `devDependencies` junto com `diex-sdk`. Veja [Estrutura do projeto → Dependências](/l/pt/developers/extend/apps/getting-started/project-structure#dependencies).

Travou? Peça ajuda no [Discord da Diex](https://discord.com/channels/1130383047699738754/1130386664812982322).
