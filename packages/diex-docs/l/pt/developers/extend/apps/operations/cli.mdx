---
title: CLI
description: comandos `yarn diex` para executar funções, transmitir logs, gerenciar instalações de aplicativos e alternar remotos.
icon: terminal
---

A CLI `yarn diex` é a sua interface para tudo relacionado a aplicativos. Lista completa de comandos:

| Comando                                         | O que faz                                                                           | Documentado em                                                                                              |
| ----------------------------------------------- | ----------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------- |
| `dev`                                           | Monitora arquivos-fonte e sincroniza alterações em tempo real                       | [Início rápido](/l/pt/developers/extend/apps/getting-started/quick-start)                                        |
| `plan`                                          | Visualize as alterações de metadados sem aplicá-las                                 | [Sincronização e recuperação](/l/pt/developers/extend/apps/operations/sync-and-recovery#previewing-changes-plan) |
| `apply`                                         | Aplicar alterações de metadados após exibir o plano                                 | [Sincronização e recuperação](/l/pt/developers/extend/apps/operations/sync-and-recovery)                         |
| `dev:build`                                     | Compile o aplicativo e gere o cliente da API (`--tarball` para empacotar um `.tgz`) | [Publicação](/l/pt/developers/extend/apps/operations/publishing)                                                 |
| `dev:typecheck`                                 | Executar a verificação de tipos do TypeScript                                       | [Testes](/l/pt/developers/extend/apps/operations/testing)                                                        |
| `dev:add`                                       | Criar a estrutura inicial de uma nova entidade                                      | [Scaffolding](/l/pt/developers/extend/apps/getting-started/scaffolding)                                          |
| `dev:generate-client`                           | Regenerar o cliente da API tipado                                                   | esta página                                                                                                 |
| `dev:function:exec` / `dev:function:logs`       | Executar funções e transmitir seus logs                                             | esta página                                                                                                 |
| `dev:translations-extract`                      | Extrair strings traduzíveis para catálogos em `locales/`                            | [Traduções](/l/pt/developers/extend/apps/translations/overview)                                                  |
| `dev:catalog-sync`                              | Acionar a sincronização do catálogo do marketplace                                  | [Publicação](/l/pt/developers/extend/apps/operations/publishing#how-marketplace-discovery-works)                 |
| `app:publish` / `app:install` / `app:uninstall` | Ciclo de vida de versões                                                            | [Publicação](/l/pt/developers/extend/apps/operations/publishing) e esta página                                   |
| `docker:*`                                      | Gerenciar o contêiner do servidor local do Diex                                   | [Servidor local](/l/pt/developers/extend/apps/getting-started/local-server)                                      |
| `remote:*`                                      | Gerenciar conexões de servidor                                                      | esta página                                                                                                 |

Todo comando aceita `-r, --remote \<name>` para direcionar a um remoto específico em vez do padrão.

## Executando funções (`yarn diex dev:function:exec`)

Execute manualmente uma função de lógica sem acioná-la via HTTP, cron ou evento de banco de dados:

```bash filename="Terminal"
# Execute by function name
yarn diex dev:function:exec -n create-new-post-card

# Execute by universalIdentifier
yarn diex dev:function:exec -u e56d363b-0bdc-4d8a-a393-6f0d1c75bdcf

# Pass a JSON payload
yarn diex dev:function:exec -n create-new-post-card -p '{"name": "Hello"}'

# Execute the install and uninstall hooks
yarn diex dev:function:exec --postInstall
yarn diex dev:function:exec --preInstall
yarn diex dev:function:exec --uninstall
```

## Visualizando logs de funções (`yarn diex dev:function:logs`)

Transmita os logs de execução das funções de lógica do seu aplicativo:

```bash filename="Terminal"
# Stream all function logs
yarn diex dev:function:logs

# Filter by function name
yarn diex dev:function:logs -n create-new-post-card

# Filter by universalIdentifier
yarn diex dev:function:logs -u e56d363b-0bdc-4d8a-a393-6f0d1c75bdcf
```

<Note>
Isso é diferente de `yarn diex docker:logs`, que mostra os logs do contêiner Docker. `yarn diex dev:function:logs` mostra os logs de execução de funções do seu aplicativo a partir do servidor Diex.
</Note>

## Gerando o cliente tipado (`yarn diex dev:generate-client`)

Regenerar o cliente de API tipado (`diex-client-sdk`) a partir do esquema do remoto ativo, sem compilar nem sincronizar um aplicativo. Use-o para obter um cliente tipado em qualquer projeto — como um serviço de backend em um repositório separado — que se comunica com a sua instância do Diex:

```bash filename="Terminal"
# In your project (no Diex app definition required)
yarn add diex-sdk diex-client-sdk

# Connect to the Diex instance to generate the client from
yarn diex remote:add

# Generate the typed client into node_modules/diex-client-sdk
yarn diex dev:generate-client
```

Em seguida, importe o cliente no seu código:

```typescript
import { CoreApiClient } from 'diex-client-sdk/core';
```

Execute novamente o comando sempre que o seu modelo de dados for alterado para atualizar os tipos gerados.

<Note>
O cliente é gerado dentro de `node_modules`, portanto não é incluído no commit junto com o seu código. Execute `yarn diex dev:generate-client` após cada instalação (por exemplo, em um script `postinstall` ou no CI).
</Note>

## Desinstalando um aplicativo (`yarn diex app:uninstall`)

Remova seu aplicativo do espaço de trabalho ativo:

```bash filename="Terminal"
yarn diex app:uninstall

# Skip the confirmation prompt
yarn diex app:uninstall --yes
```

## Gerenciando remotos

Um **remoto** é um servidor Diex ao qual seu aplicativo se conecta. Durante a configuração, o gerador de scaffold cria um para você automaticamente. Você pode adicionar mais remotos ou alternar entre eles a qualquer momento.

```bash filename="Terminal"
# Add a new remote (opens a browser for OAuth login)
yarn diex remote:add

# Connect to a local Diex server (auto-detects port 2020 or 3000)
yarn diex remote:add --local

# Add a remote non-interactively (useful for CI)
yarn diex remote:add --url https://your-diex-server.com --api-key $DIEX_API_KEY --as my-remote

# List all configured remotes
yarn diex remote:list

# Set the active remote
yarn diex remote:use <name>

# Check that the active remote's authentication is still valid
yarn diex remote:status

# Remove a remote
yarn diex remote:remove <name>
```

Suas credenciais são armazenadas em `~/.diex/config.json`.
