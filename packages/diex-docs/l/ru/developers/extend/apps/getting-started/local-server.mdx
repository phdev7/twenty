---
title: Локальный сервер
description: Управление локальным Docker-сервером Diex — запуск, остановка, обновление, параллельный тестовый экземпляр и ручная настройка SDK.
icon: server
---

## Управление локальным сервером

Используйте `yarn diex docker:*` для управления локальным контейнером Diex:

| Команда                                | Что делает                                           |
| -------------------------------------- | ---------------------------------------------------- |
| `yarn diex docker:start`             | Запустить сервер (при необходимости скачивает образ) |
| `yarn diex docker:start 2.2.0`       | Запустить конкретную версию сервера                  |
| `yarn diex docker:start --port 3030` | Запустить на пользовательском порту                  |
| `yarn diex docker:stop`              | Остановить сервер (данные сохраняются)               |
| `yarn diex docker:status`            | Показать URL, версию и учётные данные для входа      |
| `yarn diex docker:logs`              | Потоковый вывод журналов сервера                     |
| `yarn diex docker:reset`             | Стереть данные и начать заново                       |
| `yarn diex docker:upgrade`           | Скачать последний образ `diex-app-dev`             |
| `yarn diex docker:upgrade 2.2.0`     | Обновить до конкретной версии                        |

Данные сохраняются между перезапусками в двух томах Docker (`diex-app-dev-data` для PostgreSQL, `diex-app-dev-storage` для файлов). Используйте `reset`, чтобы стереть всё.

## Фиксация версии сервера

Когда версия не передана, `docker:start` определяет версию из диапазона `engines.diex` вашего приложения в `package.json` — того же диапазона, по которому сервер выполняет проверку при установке вашего приложения. Он запускает самый новый опубликованный образ `diex-app-dev`, который удовлетворяет диапазону, и использует `latest`, если поле отсутствует или ни одна опубликованная версия не совпадает:

```json filename="package.json"
{
  "engines": {
    "diex": ">=2.2.0"
  }
}
```

Передайте версию явно, чтобы переопределить диапазон для одного запуска: `yarn diex docker:start 2.3.0`. Если контейнер уже существует с другой версией, `docker:start` обновляет его на месте (пересоздаёт контейнер с сохранением томов данных).

## Обновление образа сервера

`yarn diex docker:upgrade` скачивает последний образ, сравнивает дайджесты и пересоздаёт контейнер только если действительно что-то изменилось. Тома сохраняются — заменяется только контейнер. Если был скачан новый образ и контейнер работал, при обновлении автоматически запускается новый контейнер; затем выполните `yarn diex docker:start`, чтобы дождаться его готовности.

```bash filename="Terminal"
yarn diex docker:upgrade            # Latest
yarn diex docker:upgrade 2.2.0      # Specific version
```

Проверьте запущенную версию с помощью `yarn diex docker:status` (эта команда показывает `APP_VERSION`, встроенную в контейнер).

## Запуск параллельного тестового экземпляра

Передайте `--test` любой команде `docker:*`, чтобы управлять вторым, полностью изолированным экземпляром — это полезно для запуска интеграционных тестов или экспериментов, не затрагивая ваши основные данные разработки:

| Команда                             | Что делает                                              |
| ----------------------------------- | ------------------------------------------------------- |
| `yarn diex docker:start --test`   | Запустить тестовый экземпляр (по умолчанию — порт 2021) |
| `yarn diex docker:stop --test`    | Остановить его                                          |
| `yarn diex docker:status --test`  | Показать его статус                                     |
| `yarn diex docker:logs --test`    | Транслировать его журналы                               |
| `yarn diex docker:reset --test`   | Стереть его данные                                      |
| `yarn diex docker:upgrade --test` | Обновить его образ                                      |

Тестовый экземпляр запускается в собственном контейнере Docker (`diex-app-dev-test`) с выделенными томами (`diex-app-dev-test-data`, `diex-app-dev-test-storage`) и собственной конфигурацией, поэтому он может работать параллельно с вашим основным экземпляром без конфликтов. Совместите `--test` с `--port`, чтобы переопределить значение по умолчанию (2021).

## Ручная настройка (без генератора каркаса)

Пропустите генератор каркаса, если вы добавляете SDK в существующий проект:

```bash filename="Terminal"
yarn add diex-sdk diex-client-sdk
```

Добавьте скрипт в `package.json`:

```json filename="package.json"
{
  "scripts": {
    "diex": "diex"
  }
}
```

Теперь вы можете запускать `yarn diex dev`, `yarn diex docker:start` и остальные команды.

<Note>
Не устанавливайте `diex-sdk` глобально — фиксируйте версию в каждом проекте, чтобы каждое приложение использовало свою собственную версию.
</Note>
