---
title: Yerel Sunucu
description: Yerel Diex Docker sunucusunu yönetin — başlatın, durdurun, yükseltin, paralel test örneği çalıştırın ve manuel SDK kurulumu yapın.
icon: server
---

## Yerel sunucuyu yönetme

Yerel Diex konteynerini kontrol etmek için `yarn diex docker:*` kullanın:

| Komut                                  | Ne yapar                                                  |
| -------------------------------------- | --------------------------------------------------------- |
| `yarn diex docker:start`             | Sunucuyu başlatır (gerekirse imajı çeker)                 |
| `yarn diex docker:start 2.2.0`       | Belirli bir sunucu sürümünü başlatın                      |
| `yarn diex docker:start --port 3030` | Özel bir portta başlatır                                  |
| `yarn diex docker:stop`              | Sunucuyu durdurur (verileri korur)                        |
| `yarn diex docker:status`            | URL'yi, sürümü ve oturum açma kimlik bilgilerini gösterir |
| `yarn diex docker:logs`              | Sunucu günlüklerini akış olarak gösterir                  |
| `yarn diex docker:reset`             | Verileri siler ve sıfırdan başlatır                       |
| `yarn diex docker:upgrade`           | En güncel `diex-app-dev` imajını çeker                  |
| `yarn diex docker:upgrade 2.2.0`     | Belirli bir sürüme yükseltir                              |

Veriler, yeniden başlatmalar arasında iki Docker biriminde kalıcıdır (PostgreSQL için `diex-app-dev-data`, dosyalar için `diex-app-dev-storage`). Her şeyi silmek için `reset` kullanın.

## Sunucu sürümünü sabitleme

Herhangi bir sürüm iletilmediğinde, `docker:start` sürümü uygulamanızın `package.json` içindeki `engines.diex` aralığından çözümler — bu, uygulamanız yüklendiğinde sunucunun doğruladığı aralıkla aynıdır. Bu komut, aralığı karşılayan en yeni yayımlanmış `diex-app-dev` imajını başlatır; alan yoksa veya hiçbir yayımlanmış sürüm eşleşmiyorsa `latest` değerine geri döner:

```json filename="package.json"
{
  "engines": {
    "diex": ">=2.2.0"
  }
}
```

Aralığı tek bir çalıştırma için geçersiz kılmak amacıyla sürümü açıkça iletin: `yarn diex docker:start 2.3.0`. Bir kapsayıcı zaten farklı bir sürümde mevcutsa, `docker:start` onu yerinde yükseltir (veri birimlerinizi korurken kapsayıcıyı yeniden oluşturarak).

## Sunucu imajını yükseltme

`yarn diex docker:upgrade`, en güncel imajı çeker, özetleri karşılaştırır ve yalnızca gerçekten bir şey değiştiyse konteyneri yeniden oluşturur. Birimler korunur — yalnızca konteyner değiştirilir. Yeni bir imaj çekildiyse ve konteyner çalışıyorsa, yükseltme otomatik olarak yeni bir konteyner başlatır; sağlıklı hale gelmesini beklemek için ardından `yarn diex docker:start` çalıştırın.

```bash filename="Terminal"
yarn diex docker:upgrade            # Latest
yarn diex docker:upgrade 2.2.0      # Specific version
```

Çalışan sürümü `yarn diex docker:status` ile doğrulayabilirsiniz (konteynere gömülü `APP_VERSION` değerini gösterir).

## Paralel bir test örneği çalıştırma

Ana geliştirme verilerinize dokunmadan entegrasyon testlerini veya denemeleri yürütmek için, herhangi bir `docker:*` komutuna `--test` parametresini vererek ikinci, tamamen yalıtılmış bir örneği yönetebilirsiniz:

| Komut                               | Ne yapar                                                      |
| ----------------------------------- | ------------------------------------------------------------- |
| `yarn diex docker:start --test`   | Test örneğini başlatır (varsayılan bağlantı noktası 2021'dir) |
| `yarn diex docker:stop --test`    | Durdurur                                                      |
| `yarn diex docker:status --test`  | Durumunu gösterir                                             |
| `yarn diex docker:logs --test`    | Günlüklerini akış olarak gösterir                             |
| `yarn diex docker:reset --test`   | Verilerini siler                                              |
| `yarn diex docker:upgrade --test` | İmajını yükseltir                                             |

Test örneği, kendine ait bir Docker konteynerinde (`diex-app-dev-test`), ayrılmış birimlerle (`diex-app-dev-test-data`, `diex-app-dev-test-storage`) ve yapılandırmayla çalışır; böylece ana örneğinizle çakışma olmadan paralel olarak çalışabilir. Varsayılan 2021'i geçersiz kılmak için `--test` ile `--port`'u birlikte kullanın.

## Manuel kurulum (iskelet oluşturucu olmadan)

SDK'yı mevcut bir projeye ekliyorsanız iskelet oluşturma adımını atlayın:

```bash filename="Terminal"
yarn add diex-sdk diex-client-sdk
```

`package.json` dosyasına betiği ekleyin:

```json filename="package.json"
{
  "scripts": {
    "diex": "diex"
  }
}
```

Artık `yarn diex dev`, `yarn diex docker:start` ve diğerlerini çalıştırabilirsiniz.

<Note>
`diex-sdk`'yi global olarak kurmayın — her uygulama kendi sürümünü kullansın diye proje bazında sabitleyin.
</Note>
