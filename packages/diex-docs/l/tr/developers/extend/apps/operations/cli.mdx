---
title: CLI
description: Fonksiyonları çalıştırmak, günlükleri akış olarak izlemek, uygulama kurulumlarını yönetmek ve uzaklar arasında geçiş yapmak için yarn diex komutları.
icon: terminal
---

`yarn diex` CLI, uygulama ile ilgili her şey için arayüzünüzdür. Tam komut listesi:

| Komut                                           | Ne yapar                                                                              | Şurada belgelenmiştir                                                                                      |
| ----------------------------------------------- | ------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------- |
| `dev`                                           | Kaynak dosyaları izleyin ve değişiklikleri gerçek zamanlı olarak eşitleyin            | [Hızlı Başlangıç](/l/tr/developers/extend/apps/getting-started/quick-start)                                     |
| `plan`                                          | Meta veri değişikliklerini uygulamadan önizleyin                                      | [Senkronizasyon ve kurtarma](/l/tr/developers/extend/apps/operations/sync-and-recovery#previewing-changes-plan) |
| `apply`                                         | Planı gösterdikten sonra meta veri değişikliklerini uygulayın                         | [Senkronizasyon ve kurtarma](/l/tr/developers/extend/apps/operations/sync-and-recovery)                         |
| `dev:build`                                     | Uygulamayı derleyin ve API istemcisini oluşturun (`.tgz` paketlemek için `--tarball`) | [Yayınlama](/l/tr/developers/extend/apps/operations/publishing)                                                 |
| `dev:typecheck`                                 | TypeScript tür kontrolünü çalıştırın                                                  | [Testing](/l/tr/developers/extend/apps/operations/testing)                                                      |
| `dev:add`                                       | Yeni bir varlık iskeleti oluşturun                                                    | [İskele oluşturma](/l/tr/developers/extend/apps/getting-started/scaffolding)                                    |
| `dev:generate-client`                           | Türlendirilmiş API istemcisini yeniden oluşturun                                      | bu sayfa                                                                                                   |
| `dev:function:exec` / `dev:function:logs`       | Fonksiyonları çalıştırın ve günlüklerini akış halinde alın                            | bu sayfa                                                                                                   |
| `dev:translations-extract`                      | Çevrilebilir dizeleri `locales/` kataloglarına çıkarın                                | [Çeviriler](/l/tr/developers/extend/apps/translations/overview)                                                 |
| `dev:catalog-sync`                              | Pazar yeri katalog eşitlemesini tetikleyin                                            | [Yayınlama](/l/tr/developers/extend/apps/operations/publishing#how-marketplace-discovery-works)                 |
| `app:publish` / `app:install` / `app:uninstall` | Sürüm yaşam döngüsü                                                                   | [Yayınlama](/l/tr/developers/extend/apps/operations/publishing) ve bu sayfa                                     |
| `docker:*`                                      | Yerel Diex sunucu konteynerini yönetin                                              | [Yerel Sunucu](/l/tr/developers/extend/apps/getting-started/local-server)                                       |
| `remote:*`                                      | Sunucu bağlantılarını yönetin                                                         | bu sayfa                                                                                                   |

Her komut, varsayılan yerine belirli bir uzak sunucuyu hedeflemek için `-r, --remote \<name>` parametresini kabul eder.

## Fonksiyonları çalıştırma (`yarn diex dev:function:exec`)

Bir mantık fonksiyonunu HTTP, cron veya veritabanı olayıyla tetiklemeden manuel olarak çalıştırın:

```bash filename="Terminal"
# Execute by function name
yarn diex dev:function:exec -n create-new-post-card

# Execute by universalIdentifier
yarn diex dev:function:exec -u e56d363b-0bdc-4d8a-a393-6f0d1c75bdcf

# Pass a JSON payload
yarn diex dev:function:exec -n create-new-post-card -p '{"name": "Hello"}'

# Execute the install and uninstall hooks
yarn diex dev:function:exec --postInstall
yarn diex dev:function:exec --preInstall
yarn diex dev:function:exec --uninstall
```

## Fonksiyon günlüklerini görüntüleme (`yarn diex dev:function:logs`)

Uygulamanızın mantık fonksiyonlarının yürütme günlüklerini akış olarak alın:

```bash filename="Terminal"
# Stream all function logs
yarn diex dev:function:logs

# Filter by function name
yarn diex dev:function:logs -n create-new-post-card

# Filter by universalIdentifier
yarn diex dev:function:logs -u e56d363b-0bdc-4d8a-a393-6f0d1c75bdcf
```

<Note>
Bu, Docker konteyner günlüklerini gösteren `yarn diex docker:logs` komutundan farklıdır. `yarn diex dev:function:logs`, uygulamanızın fonksiyon yürütme günlüklerini Diex sunucusundan gösterir.
</Note>

## Tiplendirilmiş istemciyi oluşturma (`yarn diex dev:generate-client`)

Bir uygulamayı oluşturup eşitlemeden, etkin uzak sunucunun şemasından tiplendirilmiş API istemcisini (`diex-client-sdk`) yeniden oluşturun. Bunu, Diex örneğinizle iletişim kuran — örneğin ayrı bir depoda bulunan bir arka uç hizmeti — herhangi bir projede tiplendirilmiş bir istemci edinmek için kullanın:

```bash filename="Terminal"
# In your project (no Diex app definition required)
yarn add diex-sdk diex-client-sdk

# Connect to the Diex instance to generate the client from
yarn diex remote:add

# Generate the typed client into node_modules/diex-client-sdk
yarn diex dev:generate-client
```

Ardından istemciyi kodunuza içe aktarın:

```typescript
import { CoreApiClient } from 'diex-client-sdk/core';
```

Oluşturulan türleri yenilemek için veri modeliniz her değiştiğinde komutu yeniden çalıştırın.

<Note>
İstemci `node_modules` içinde oluşturulur, bu nedenle kodunuzla birlikte commit edilmez. Her kurulumdan sonra (örneğin bir `postinstall` betiğinde veya CI içinde) `yarn diex dev:generate-client` komutunu çalıştırın.
</Note>

## Bir uygulamayı kaldırma (`yarn diex app:uninstall`)

Uygulamanızı etkin çalışma alanından kaldırın:

```bash filename="Terminal"
yarn diex app:uninstall

# Skip the confirmation prompt
yarn diex app:uninstall --yes
```

## Uzakları yönetme

Bir **uzak**, uygulamanızın bağlandığı Diex sunucusudur. Kurulum sırasında iskelet oluşturucu sizin için otomatik olarak bir tane oluşturur. Dilediğiniz zaman daha fazla uzak ekleyebilir veya aralarında geçiş yapabilirsiniz.

```bash filename="Terminal"
# Add a new remote (opens a browser for OAuth login)
yarn diex remote:add

# Connect to a local Diex server (auto-detects port 2020 or 3000)
yarn diex remote:add --local

# Add a remote non-interactively (useful for CI)
yarn diex remote:add --url https://your-diex-server.com --api-key $DIEX_API_KEY --as my-remote

# List all configured remotes
yarn diex remote:list

# Set the active remote
yarn diex remote:use <name>

# Check that the active remote's authentication is still valid
yarn diex remote:status

# Remove a remote
yarn diex remote:remove <name>
```

Kimlik bilgileriniz `~/.diex/config.json` içinde saklanır.
