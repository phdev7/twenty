// Upstream enforces this as a hardcoded ceiling. Here it is only the default
// for the MAX_WORKSPACES_WITHOUT_ENTERPRISE_KEY config variable, so the limit
// can be raised from configuration instead of a rebuild.
export const MAX_WORKSPACES_WITHOUT_ENTERPRISE_KEY_DEFAULT = 5;
