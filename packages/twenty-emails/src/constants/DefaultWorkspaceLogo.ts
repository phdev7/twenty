// next-crm.bydiex.com was the pre-migration host and now answers 503, so this
// logo was broken in every email that fell back to it.
export const DEFAULT_WORKSPACE_LOGO =
  'https://app.crm.bydiex.com/images/brand/logomark.svg';
